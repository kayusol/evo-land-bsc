{
  "name": "evo-land-bsc",
  "version": "2.0.0",
  "description": "Evolution Land BSC - Simplified Single Chain",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network bscTestnet",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.22.0"
  }
}
